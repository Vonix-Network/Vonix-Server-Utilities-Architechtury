# AGENTS.md — workspace guide for vonix_server_utils-1.21.1-fabric-neoforgetemplate

## Workspace identity

- Path: `/root/DEV/Vonix-Server-Utilities-Architechtury/vonix_server_utils-1.21.1-fabric-neoforgetemplate`
- Category: `minecraft/java-mod`
- Tags: `vonix`
- Git remote: `none/local-only`
- Current branch when generated: `n/a`
- Marker files: `CHANGELOG.md, gradle.properties, settings.gradle`

## First-read order for agents

1. `AGENTS.md` — this file.
2. `README.md` if present — user-facing overview.
3. `CHANGELOG.md` / release notes if present — shipped behavior and SemVer history.
4. Build manifests (`package.json`, `Cargo.toml`, `pyproject.toml`, `gradle.properties`, `settings.gradle`, `pom.xml`) before guessing commands.
5. Existing project docs (`docs/`, `CLAUDE.md`, `Handoff.md`, `LaunchRoadmap.md`) when present.

## Operating rules

- Start every change with `git status --short` and preserve unrelated dirty work.
- Prefer targeted reads/searches over dumping large files.
- Never read or print `.env`, private keys, token files, live service config with secrets, database URLs, or authorization headers. Check structure/key presence only.
- Do not fabricate commands, env vars, config keys, or deployment steps; derive them from source or docs.
- Keep documentation changes grounded in actual files and commands.
- Add or update tests when changing behavior; if no test framework is present, record the manual verification used.

- Follow Vonix mod rules: strict SemVer, Keep-a-Changelog, JarInJar for JDBC/JNI/shared deps, and verify jar metadata before deploy.
- Live server deployment is a separate Pterodactyl workflow; do not claim deployment from a local build alone.

## Useful local commands

- `chmod +x ./gradlew || true`
- `./gradlew build -x test  # if this repo has multi-cell profiles, prefer the scoped profile documented in README/CHANGELOG`
- `unzip -l build/libs/*.jar | grep -E 'META-INF/(jarjar|mods.toml|neoforge.mods.toml)'  # verify packaging when shipping`

## Completion checklist

- `git status --short` reviewed; unrelated changes preserved.
- Relevant build/test/lint command run or a blocker recorded.
- New/changed behavior documented in README/docs/CHANGELOG when appropriate.
- Secrets stayed out of logs, diffs, tool output, and commit messages.
- If this workspace deploys to production, runtime health was verified after deploy.

---

Generated by Hermes workspace inventory on 2026-07-03 05:10:56Z. Regenerate with `/root/.hermes/scripts/workspace_system_inventory.py`.
