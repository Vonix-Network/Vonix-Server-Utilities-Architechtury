# Playtime and Event Crates — Operator Runbook

VSU 1.7.0 adds command-based typed virtual keys and persistent weighted crates. Use `/crate key give` for event distribution, `/crate prizes` for percentages, and `/crate open` for server-authoritative opening.
